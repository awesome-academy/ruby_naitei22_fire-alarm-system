en:
  activerecord:
    models:
      user: "User"
      invitation: "Invitation"
    attributes:
      invitation:
        code: "Code"
        purpose: "Purpose"
        used: "Used"
      user:
        admin:
          must_exist_for_supervisor: "must exist for a supervisor"
        admin_id:
          cannot_be_set_for_admin: "cannot be set for an admin user"
